<!DOCTYPE html>
<html lang="en" class="no-js">

    <head>
        {% include "partials/meta.njk" %}
    </head>

    <body>
        <!-- NAVBAR -->
        {% include "partials/navbar.njk" %}

        <!-- SPLASH -->
        {% include "partials/splash-screen.njk" %}

        <!-- ME -->
        {% include "partials/intro.njk" %}

        <!-- MISSION -->
        {% include "partials/mission.njk" %}

        <!-- CONTACT -->
        {% include "partials/contact.njk" %}

        <!-- FOOTER -->
        {% include "partials/footer.njk" %}

        <!-- JAVASCRIPTS -->
        {% include "partials/js.njk" %}
    </body>

</html>