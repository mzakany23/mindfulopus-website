# mindfulopus notes

## 1. how to boot up the site

```bash
npm run develop 
```

## 2. how to change the site

command p to look for files
change file
save file (command s)

## 3. how to push/deploy

to push the site changes, these are the commands to run:

```bash
git add . && git commit -m "add text" && git push 
```